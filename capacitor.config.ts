import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kalyesilog.pos',
  appName: 'Kalye Silog&Sizzling',
  webDir: 'dist/kalye-silog/browser',
};

export default config;
